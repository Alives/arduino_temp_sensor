* Last working compile library versions are defined in `arduino_temp_sensor.ino` and `wifi.ino`.
* After getting the IDE setup, set the Flash Size to FS:1MB for SPIFFS storage.
* Serial speed 115200.
